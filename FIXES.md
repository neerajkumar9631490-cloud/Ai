# MYRAA Device QA Defect Resolution Matrix

| Defect ID | Defect Description | Root Cause | Exact Resolution & Code Change | Files Modified |
| :--- | :--- | :--- | :--- | :--- |
| **D1** | Camera & notification permissions prompted aggressively at initial app startup. | `MainActivity.kt` requested `RECORD_AUDIO`, `CAMERA`, and `POST_NOTIFICATIONS` in an eager startup array inside `onCreate`. | Removed `CAMERA` and `POST_NOTIFICATIONS` from startup. Permissions are now strictly gated on demand: `RECORD_AUDIO` on first mic click/SetupWizard step 3, `POST_NOTIFICATIONS` only when wake-word background service is enabled, and `CAMERA` only during the `take_photo` tool intent execution. Updated `AndroidManifest.xml` accordingly. | `MainActivity.kt`, `AndroidManifest.xml`, `SystemBridgePlugin.kt` |
| **D2** | Avatar canvas is empty black because missing `character.vrm` failed silently without fallback. | Avatar loader had no fallback mechanism when `character.vrm` was absent, leaving WebGL with an empty black void. | 1. Wrapped VRM loading in safe `try/catch`.<br/>2. Implemented procedural `FallbackHologram` (animated 3D particle point-cloud and glowing humanoid wireframe silhouette with floating starfield and cyan/violet idle breathing).<br/>3. Added a persistent, non-intrusive runtime "CHARACTER CORE MISSING — Import .vrm" card with file picker supporting runtime `.vrm` upload & local preference persistence.<br/>4. Logged exact VRM load warnings and errors directly to `DebugConsole` and `Toasts`. | `web/src/avatar/Avatar.ts`, `app/src/main/assets/public/index.html` |
| **D3** | Input placeholder displayed literal brackets `[MYRAA]`. | Placeholder string was hardcoded with square brackets. | Created `src/config.ts` exporting centralized `CONFIG` (`appName: 'MYRAA'`, `nickname: 'TECH'`, etc.). Removed and strictly banned literal brackets across all user-facing UI elements, placeholders, and labels. Placeholder now dynamically reads `Type a message to MYRAA…`. | `web/src/config.ts`, `web/src/main.ts`, `app/src/main/assets/public/index.html` |
| **D4** | Status pill overflowed on narrow screens and displayed tofu glyphs (⍰ / ⏻ / 🔊). | Pill lacked `max-width` constraints and CSS marquee overflow handling, and used emoji/unicode glyphs unsupported on some Android fonts. | 1. Applied `max-width: min(92vw, 560px)` and contained CSS marquee `translateX` animation on text overflow.<br/>2. Replaced 100% of emoji, unicode, and icon fonts with crisp, lightweight inline SVG components for power, mic, speaker, settings, send, status, copy, and close. | `web/src/styles.css`, `web/src/ui/HUD.ts`, `app/src/main/assets/public/index.html` |
| **D5** | Send button cleared input field and failed silently without feedback. | Chat send handler had no exception handling, no connection check, and no user-facing error reporting. | 1. Branching logic: If no API key configured → opens `SetupWizard` at Step 1.<br/>2. If key configured → ensures active Gemini Live WebSocket session and sends `clientContent` turn.<br/>3. Wrapped all communication and HTTP/WS events with real-time error toasts (e.g., `401 API key invalid`, `Network timeout`) and rendered user turns and AI replies in subtitle cards with spoken audio. | `web/src/ai/GeminiLive.ts`, `web/src/main.ts`, `app/src/main/assets/public/index.html` |
| **D6** | "Connect Memory Core" was unreachable and no first-run onboarding existed. | The app lacked a guided multi-step onboarding wizard. | Created `ui/SetupWizard.ts` with 4 interactive steps & progress dots:<br/>• Step 1: Gemini API Key entry with live `TEST CONNECTION` button & hardware AES-256 vault status.<br/>• Step 2: Character VRM import or Continue with Hologram.<br/>• Step 3: Microphone permission grant.<br/>• Step 4: "Say hello" interactive test turn. Accessible on boot and anytime via status pill / power button. | `web/src/ui/SetupWizard.ts`, `web/src/main.ts`, `app/src/main/assets/public/index.html` |
| **D7** | Missing HUD controls and non-responsive buttons. | UI lacked full HUD implementation and Push-to-Talk visual feedback. | Implemented `ui/HUD.ts` featuring interactive SVG Power button, responsive Status Pill with marquee, SVG Speaker mute toggle with state icon, SVG Settings gear opening the 5-tab modal, and Restyled Push-to-Talk button with tactile pressed states, glowing pulse ring, and 16 kHz stream initiation on `pointerdown` and completion on `pointerup`. | `web/src/ui/HUD.ts`, `web/src/styles.css`, `app/src/main/assets/public/index.html` |
| **D8** | Wrong theme used lavender buttons on black canvas. | The previous theme had lavender `#D0BCFF` styling instead of dark-cinematic cyberpunk aesthetic. | Redesigned full CSS system to dark-cinematic theme: Background `#0b0f14`, glassmorphic panels `rgba(255, 255, 255, 0.06)`, primary accent Cyan `#22d3ee`, secondary accent Violet `#7c3aed`, glowing border accents `rgba(34, 211, 238, 0.25)`, and monospace status typography. Removed all lavender palette. | `web/src/styles.css`, `app/src/main/assets/public/index.html` |
| **D9** | No error visibility for runtime exceptions or network failures. | Failures were logged only to browser console without UI indicators. | Built `ui/Toasts.ts` (stacked auto-dismiss toast notifications with info, success, warning, and error styles) and `DebugConsole` (collapsible modal storing last 30 log lines with copy button, wired to `window.onerror`, `window.onunhandledrejection`, and WebSocket error handlers). | `web/src/ui/Toasts.ts`, `web/src/ui/Settings.ts`, `app/src/main/assets/public/index.html` |
